import Table from './components/Table';
import { BrowserRouter } from 'react-router-dom';

const App = () => {
  
  return (
    <BrowserRouter>
      <Table />
    </BrowserRouter>
  );
};

export default App;
